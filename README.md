# Pokédex CLI

A command-line Pokédex built in Go that lets you look up information about Pokémon using the [PokéAPI](https://pokeapi.co/).

## Features

- Interactive REPL (Read-Eval-Print Loop) interface
- Fetch Pokémon data via HTTP requests to PokéAPI
- Response caching for improved performance
- JSON parsing and structured data display